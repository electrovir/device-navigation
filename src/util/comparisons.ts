/**
 * Just a wrapper for `>`.
 *
 * @category Internals
 */
export function greaterThan(a: any, b: any) {
    return a > b;
}

/**
 * Just a wrapper for `>`.
 *
 * @category Internals
 */
export function lessThan(a: any, b: any) {
    return a < b;
}
